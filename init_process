#include "monty.h"

StackNode *stack = NULL; // Initialize the stack pointer

void init_stack(void) {
    // You can set `stack` to NULL here to start with an empty stack
    stack = NULL;
}

void process_line(char *line) {
    (void)line;
}
